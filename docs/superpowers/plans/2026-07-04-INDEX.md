# Skill↔Talent + Adversary Plans — INDEX (status 2026-07-07)

**Read this file first. Open only the ONE plan you are implementing. Never open plans marked ✅.**

## Token-lean implementation protocol

1. Each plan file is self-contained (file map + verbatim code + test commands). Do **not** read the
   full design specs — when a plan cites the spec, open only the named section of
   `../specs/2026-07-04-skill-talent-link-design.md` / `…-adversary-skill-trait-link-design.md`.
2. Trust the ✅/🔶 markers and this table; do not re-verify finished plans by re-reading source.
3. Data-only specs run without a browser and in ~2s:
   `npx playwright test tests/id-uniqueness.spec.ts tests/talent-skill-refs.spec.ts tests/skill-talent-index.spec.ts tests/adversary-refs.spec.ts tests/adversary-references.spec.ts tests/talents-data.spec.ts tests/talent-references.spec.ts tests/skills-data.spec.ts --project=chromium`
   (all 21 green as of 2026-07-07). Run the full browser suite only at a plan's final verify step.
4. One plan (or one batch) per commit. When done, mark the plan's header ✅/🔶 with the commit hash
   and update the table below. That is what keeps the next session cheap.
5. All catalog ids are prefixed (`skill_*`, `talent_*`, `trait_*`) since Plan 00 — author new refs
   with prefixes.

## Status

| Plan | Status | Remaining |
|---|---|---|
| 00 unique-ids | ✅ | — |
| 01 helpers | ✅ | — |
| 02 type-fields | ✅ | — |
| 02b effect-registry | ⬜ | all (absorbs old Plan 08) |
| 03 reconcile-max | ✅ | — (fixed on main, lock test green) |
| 04 remove-non-core | ✅ | — |
| 05 reconcile-mechanics | 🔶 | accurate_shot typed effect; nimble_fingered/savvy/very_resilient remodel |
| 06 backfill-typed | 🔶 | rest of Batch 1 + Batches 2–4 + handler tests — **after 02b** |
| 07 backfill-prose | ✅ | — (83 refs + ratchet green) |
| 08 matcher | ✅ | superseded by 02b, nothing to do |
| 09 thread-roller | ⬜ | all — after 02b |
| 10 grants-career-xp | ⬜ | all (needs `grantsSkillIds` data from 06 mapping C) |
| 11 ui-talent-to-skill | ✅ | — |
| 12 ui-skill-to-talent | 🔶 | UI render only (core index + tests done) |
| 13 drop-fallback | ⬜ | all — last, after 06+07+09 ratchet green |
| A01 parsers | ✅ | — |
| A02 traits-field | ✅ | — |
| A03 de-conflate | ✅ | — |
| A04 validation | ✅ | — |
| A05 reconcile-traits | ⬜ | spot-reconcile vs bestiary.md (A04 already resolves) |
| A06 trait-effects | ⬜ | all — after 02b |
| A07 creature-skill-refs | ⬜ | all |
| A08 adversary-ui | ⬜ | all — after A05–A07 + 11/12 |

## Remaining queue (dependency order)

1. **02b** effect registry — unblocks 06, 09, 13, A06. The only architecture-heavy plan left.
2. **05** remainder (small, independent — can go before/after 02b)
3. **06** remainder (batches, each own commit)
4. **09** thread roller
5. **10** grants/career-XP
6. **11**, **12** remainder (UI pair)
7. **A05**, **A07** (small, independent), then **A06**
8. **13** drop fallback (only when 07's ratchet + 09's spec are green)
9. **A08** adversary UI (last)

## Model guidance (fewest tokens per plan)

- **Sonnet 5** for everything in the queue by default — the plans are prescriptive enough
  (file maps, verbatim snippets, named ids) that a mid-tier model executes them without exploration.
- **Opus 4.8** only for Plan **02b** (the one real refactor with cross-file behavior preservation)
  and for debugging if a suite goes red for non-obvious reasons.
- **Haiku 4.5** is enough for pure list-application work (05 remainder, A05 spot fixes) if you're
  batching costs, but Sonnet 5 is the safe default for anything touching WFRP rules interpretation.
- Writing/revising plans or specs: Opus 4.8 / Fable 5. Don't spend that tier on execution.
